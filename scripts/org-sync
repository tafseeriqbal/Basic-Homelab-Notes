#!/bin/sh
cd "$HOME/org" || exit 1
git pull --rebase --quiet 2>/dev/null
if [ -n "$(git status --porcelain)" ]; then
  git add -A
  git commit -q -m "sync $(date '+%F %H:%M')"
fi
git push --quiet 2>/dev/null
